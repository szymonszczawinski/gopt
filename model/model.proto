syntax = "proto3";
option go_package = ".;model";
package model;
 enum ProjectStatus {
    NEW = 0;
    OPEN = 1;
    CLOSED = 2;
  }

message Project {
    string name = 1;
    int32 id = 2;  // Unique ID number for this person.
   
    ProjectStatus status = 3;
    string name2 = 4;
    }
